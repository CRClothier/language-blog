<%= render partial: 'post', locals: { post: @post, display_comments: true } %>

<%= button_to 'Like button', user_post_likes_path(@post.user_id, @post) %>

<%= render '/comments/new' %>

<%= link_to 'Back to posts', user_posts_path(@post.user_id) %> | 
<%= link_to 'Back to users', root_path %>